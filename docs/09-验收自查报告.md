---
doc_id: ACCEPT-POM-V1.0
title: POM V1.0 MVP 验收自查报告
owner: POM 项目组
status: DONE
last_reviewed: 2026-09-10
source_of_truth: false
related_docs:
  - docs/03-功能详细设计.md
  - docs/08-研发任务清单.md
---

# POM V1.0 MVP 验收自查报告

> 对照 PRD 9.2/9.3、附录 B、docs/03 5.1 权限矩阵逐项自查。检查日期：2026-09-10。

## 1. 自动化测试结论

| 域 | 测试文件 | 用例数 | 结果 |
| --- | --- | --- | --- |
| 认证/权限/会话 | test_auth_api.py | 10 | 全绿 |
| 导入引擎 + 三样例回归 | test_samples_regression.py | 10 | 全绿 |
| 全链路冒烟（API 级） | test_smoke.py | 4 | 全绿 |
| 合计 | — | 24 | **24 passed** |

## 2. PRD 9.2 MVP 总体验收

| # | 条目 | 结论 | 证据 |
| --- | --- | --- | --- |
| 1 | 三类角色按权限登录使用，未授权操作后端拒绝 | ✓ | test_role_forbidden（viewer/maintainer 访问用户管理 403；viewer 导入 403） |
| 2 | 三份样例 xls 完成上传/预览/映射/导入，不改原文件 | ✓ | test_samples_readable_and_template_detected 等 10 例；UI 走查已入库 |
| 3 | 一源行多家供应商可拆分并查询每条报价 | ✓ | test_sample2_horizontal_split（刘/曾/胡 组拆分） |
| 4 | 名称/编码/型号/品牌/供应商可返回历史记录 | ✓ | 引擎命中断言 + search 多域关键词（test_full_chain_smoke 步骤2） |
| 5 | 每条结构化结果显示来源文件/工作表/行号/原始文本 | ✓ | test_full_chain_smoke 步骤3（raw_cells 断言）；quote.source 非空由入库约束保证 |
| 6 | 修改/软删除有审计，原始快照不可覆盖 | ✓ | test_full_chain_smoke 步骤5-8 + PATCH 白名单 40402（R-MNT-01） |
| 7 | 备份可执行，独立环境恢复验证 | ⚠ 部分 | 脚本与手册齐备（deploy/backup）；实机演练需部署服务器执行（本机无 Docker，RSK-001） |
| 8 | HTTPS 访问；DB 与文件目录不可公网直连 | ⚠ 部分 | Compose 内 DB 仅 expose 不映射公网桶；HTTPS 依赖云 LB（服务器无 Docker，实施阶段落实） |

## 3. PRD 附录 B 导入验收检查表

| # | 检查项 | 结论 | 断言 |
| --- | --- | --- | --- |
| B1 | 文件可读取，工作表数量一致 | ✓ | sheets 输出含全部工作表 |
| B2 | 表头识别正确，映射可修正 | ✓ | 模板第 3 行自动识别；映射接口支持人工改 |
| B3 | 名称/编码/规格/数量/单位无整体错位 | ✓ | 样例1 位移检测（assert shift_detected + material_code 断言） |
| B4 | 有效价转数值，无效价不强制转换 | ✓ | 样例3"频率要5600"不生成报价；无金额行保留原文 |
| B5 | 多供应商拆分指向同一原始行 | ✓ | 兄弟报价共享 source_row_snapshot_id（入库代码+样例2断言） |
| B6 | 原备注/文件名/表/行号完整保留 | ✓ | r1.cells 含"单价530"断言 |
| B7 | 重复文件不静默重复入库 | ✓ | test_duplicate_upload_blocked（duplicate + 原批次链接） |
| B8 | 失败/待核对项给出明确原因 | ✓ | 报告 items[].reasons（校验/位移/待核对原因） |

## 4. 权限矩阵走查（docs/03 5.1）

| 角色 | 查询/详情 | 导入/批次 | 纠错/删除 | 用户/审计/备份 | 敏感字段 |
| --- | --- | --- | --- | --- | --- |
| user | ✓ | 403 ✓ | 403 ✓ | 403 + 菜单隐藏 ✓ | 脱敏 + masked_fields ✓ |
| maintainer | ✓ | ✓ | ✓ | 403 ✓ | 明文 + SENSITIVE_VIEW 审计 ✓ |
| admin | ✓ | 全部批次 ✓ | 恢复 ✓ | ✓ | 明文 ✓ |

## 5. 遗留项（不阻塞 MVP 验收）

1. **实机部署演练**（RSK-001/RSK-004）：本机无 Docker；部署后按 deploy/backup/RESTORE.md 完成恢复演练并回填记录表。
2. **HTTPS 入口**：依赖服务器与证书选型（PRD 10.3-9，实施决策）。
3. **V1.1 范围**：比价、导出、移动端深度适配（docs/01 §5）。
4. **待确认事项**：报价日期优先级等 4 项（docs/03 §8），当前按文档记录的默认值实现。

## 6. 结论

全部 P0 功能走通并通过自动化验证：**V1.0 MVP 验收通过**（备份/HTTPS 两项为部署侧待办，二进制交付物与手册齐备）。
